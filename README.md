# 📅 Meo Bot - Бот для отслеживания посещаемости мероприятия

Телеграм бот на aiogram 3 + aiosqlite для отслеживания посещаемости 5-дневного мероприятия.

## 🚀 Возможности

- ✅ Регистрация участников (ФИО + группа)
- ✅ Отслеживание посещаемости по дням
- ✅ Коды дня для отметки присутствия
- ✅ Админ-панель для управления
- ✅ Рассылка сообщений участникам
- ✅ Полный отчёт со статистикой в боте

## 📦 Установка

1. **Создайте виртуальное окружение:**

```bash
cd meo_bot
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или venv\Scripts\activate  # Windows
```

2. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

3. **Создайте файл `.env` с настройками:**

```env
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=123456789,987654321
```

- `BOT_TOKEN` - токен бота от @BotFather
- `ADMIN_IDS` - ID администраторов через запятую (узнать свой ID можно у @userinfobot)

## 🎮 Запуск

```bash
python main.py
```

## 📱 Использование

### Для участников:
1. Отправьте `/start` для регистрации
2. Введите ФИО и группу
3. Когда администратор откроет день - введите код для отметки
4. Смотрите свою статистику в меню

### Для администраторов:
1. Отправьте `/admin` для входа в админ-панель
2. **📅 Открыть новый день** - выберите день и задайте код
3. **🔒 Закрыть день** - отключить возможность отметки
4. **📊 Статистика** - посмотреть данные по дням
5. **👥 Список участников** - краткий список
6. **📋 Полный отчёт** - подробный отчёт со всеми данными
7. **📨 Рассылка** - отправить сообщение всем участникам

## 📁 Структура проекта

```
meo_bot/
├── main.py           # Точка входа
├── config.py         # Конфигурация
├── database.py       # Работа с SQLite (асинхронно)
├── keyboards.py      # Клавиатуры
├── handlers/
│   ├── __init__.py
│   ├── user.py       # Обработчики для участников
│   └── admin.py      # Админ-панель
├── requirements.txt
└── .env              # Настройки (создать самому)
```

## 💾 База данных

SQLite база создаётся автоматически при первом запуске. Содержит:
- `users` - участники (user_id, ФИО, группа)
- `event_days` - дни мероприятия (номер, код, статус)
- `attendance` - посещения (user_id, день)

## 🔒 Логика работы

1. Участник регистрируется через /start
2. Админ через /admin открывает день с уникальным кодом
3. Участники вводят код - получают отметку
4. В один день работает только один код
5. За прошлые дни отметиться нельзя
6. Админ может закрыть день и открыть следующий
