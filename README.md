# 📅 Meo Bot - Бот для отслеживания посещаемости мероприятия

Телеграм бот на aiogram 3 + aiosqlite для отслеживания посещаемости 5-дневного мероприятия.
Включает Mini App с QR-сканером для быстрой отметки.

## 🚀 Возможности

- ✅ Регистрация участников (ФИО + группа)
- ✅ Отслеживание посещаемости по дням
- ✅ **QR-сканер** через Telegram Mini App
- ✅ Ручной ввод кода как альтернатива
- ✅ Админ-панель для управления
- ✅ Рассылка сообщений участникам
- ✅ Полный отчёт со статистикой в боте

## 📦 Установка

### 1. Бот

```bash
cd meo_bot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Создайте `.env` файл:

```env
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=922109605
WEBAPP_URL=https://username.github.io/meo-webapp/
```

### 3. Деплой Mini App на GitHub Pages

1. Создайте новый репозиторий на GitHub (например `meo-webapp`)
2. Загрузите содержимое папки `webapp/` в репозиторий
3. Включите GitHub Pages:
   - Settings → Pages → Source: Deploy from branch → `main` / `root`
4. Дождитесь деплоя (1-2 минуты)
5. URL будет: `https://username.github.io/meo-webapp/`
6. Укажите этот URL в `.env` как `WEBAPP_URL`

## 🐳 Docker

```bash
# Создать .env файл
echo "BOT_TOKEN=токен
ADMIN_IDS=922109605
WEBAPP_URL=https://username.github.io/meo-webapp/" > .env

# Запустить
docker compose up -d --build

# Логи
docker compose logs -f
```

## 📱 Использование

### Для участников:
1. `/start` — регистрация
2. **📷 Сканировать QR** — открывает камеру, сканирует код дня
3. **📝 Ввести код вручную** — ввод кода текстом
4. **📊 Моя статистика** — посмотреть посещения

### Для администраторов:
1. `/admin` — админ-панель
2. **📅 Открыть новый день** — выбрать день и задать код
3. **🔒 Закрыть день** — отключить отметки
4. **📋 Полный отчёт** — подробная статистика
5. **📨 Рассылка** — сообщение всем участникам

### QR-коды:
Для каждого дня создайте QR-код с текстом кода дня.
Например, если код дня `ДЕНЬ1`, создайте QR с текстом `ДЕНЬ1`.

Генератор QR: https://www.qr-code-generator.com/

## 📁 Структура проекта

```
meo_bot/
├── main.py              # Точка входа
├── config.py            # Конфигурация
├── database.py          # SQLite (асинхронно)
├── keyboards.py         # Клавиатуры
├── handlers/
│   ├── user.py          # Регистрация, QR, статистика
│   └── admin.py         # Админ-панель
├── webapp/              # Mini App (для GitHub Pages)
│   ├── index.html
│   ├── style.css
│   └── app.js
├── Dockerfile
├── docker-compose.yml
└── requirements.txt
```

## 🎨 Кастомизация Mini App

Откройте `webapp/style.css` и измените переменные в начале файла:

```css
:root {
    /* Основные цвета - ЗАМЕНИ НА ЦВЕТА ИЗ БРЕНДБУКА */
    --color-primary: #6C5CE7;
    --color-primary-dark: #5B4BC7;
    --color-primary-light: #A29BFE;
    ...
}
```

Также можете заменить логотип в `webapp/index.html`:
```html
<span class="logo-icon">📅</span>
<!-- Или вставить картинку: <img src="logo.png" alt="Logo"> -->
```

## 🔒 Логика работы

1. Участник регистрируется через /start
2. Админ открывает день с кодом (например `ДЕНЬ1`)
3. Админ генерирует QR-код с текстом `ДЕНЬ1`
4. Участники сканируют QR → автоматическая отметка
5. Или вводят код вручную
6. Админ может закрыть день и открыть следующий
